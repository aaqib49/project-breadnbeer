language: java
jdk:
    - openjdk8
group: stable
dist: trusty
os: linux
sudo: false
install: true
addons:
  sonarcloud:
    organization: "breadnbeer"
     branches:
     - ".*"
    token:
      secure: ${pass}
    github_token:
      secure: ${breadnbeer}
script:
  - "gradle sonarqube"
